var requestURL;
let request = new new XMLHttpRequest();

function choose1(choice) {
	// body...
	switch (choice) {
	 	case 'movies':
	 		// statements_1
	 		requestURL = '';
	 		break;
	 	
	 	default:
	 		// statements_def
	 		break;
	 } 

	
}
